{
  "_comment": "Nearest tsconfig for Vite when web/ imports these files. Empty include so Expo still owns them via ../tsconfig.json (extends expo). Do not add extends: expo here — Vercel web install has no expo.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noEmit": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "useDefineForClassFields": true
  },
  "include": [],
  "files": []
}
