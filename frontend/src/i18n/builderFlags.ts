/**
 * Builders: set true to surface the language picker (app header + Profile,
 * web header). Off in this template — UI stays English; zh/ko locale files
 * still ship so flipping this on is enough.
 */
export const SHOW_LANGUAGE_UI = false;
